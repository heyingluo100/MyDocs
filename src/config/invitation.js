// ════════════════════════════════════════════
// 邀请码系统配置
// 使用 npm run gen-code 生成邀请码和哈希值
// ════════════════════════════════════════════

export default {
  // ═══ 全站锁 ═══
  // 开启后，访问任何页面都需要先输入邀请码
  global: {
    enabled: false,
    codeHash: '',  // SHA-256 哈希值（由 npm run gen-code 生成）
  },

  // ═══ 单篇锁 ═══
  // 开启后，带 .lock 文件的文章需要额外输入邀请码
  article: {
    enabled: true,
  }
}
