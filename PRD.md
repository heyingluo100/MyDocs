# 个人文档库 — 产品需求文档（PRD）

## 1. 产品概述

| 项目 | 说明 |
|------|------|
| 产品名称 | 个人文档库 |
| 产品类型 | 静态个人文档存档网站 |
| 目标用户 | 站主（管理）+ 访客（只读浏览） |
| 部署方式 | GitHub Pages / Vercel（纯静态） |
| 技术栈 | Vue 3 + Vite + Tailwind CSS 4 |

## 2. 核心需求

### 2.1 内容管理（文件夹即标签）
- 在 `content/` 下创建文件夹，**文件夹名就是分类标签**
- 将 `.docx`（Word）或 `.md`（Markdown）文件拖入文件夹即完成发布
- `.doc`（旧版 Word 97-2003）文件通过 word-extractor 提取文本并渲染
- `.txt` 文本文件内容直接渲染在页面上
- `.pdf` 等附件文件会自动复制到公开目录供预览
- Word 文件通过 mammoth.js 在构建时转为 HTML，**精确保留空行和段落间距**
- 文档中的空格和换行在页面上完整保留（`white-space: pre-wrap`）
- 构建时自动裁掉文档末尾空段落，保留文中空行
- 构建时自动扫描所有分类文件夹，生成文章索引 JSON
- 自动跳过 Word 临时文件（`~$` 开头）
- **合集功能**：标签文件夹下的子目录自动识别为合集，合集内文章按文件名排序
- **多标签**：合集文件夹内放 `.tags` 文件（文件名就叫 `.tags`），单独文件旁放 `文件名.tags`（如 `报告.tags` 对应 `报告.docx`），每行写一个额外标签名
- **双时间字段**：创建时间（fs.birthtime / .docx 内部元数据）+ 更新时间（mtime），按创建时间倒序排列
- 示例：`content/工作/报告.docx` → 标签为「工作」，标题为「报告」

### 2.2 内容展示
- 首页展示所有文章卡片列表（标题、摘要、标签、日期、合集标记）
- 合集以堆叠卡片样式混排在文章网格中
- 文章按创建时间倒序排列
- 弹出面板式标签筛选（桌面弹出面板 + 移动端底部抽屉）
- 全局标题搜索，跟随当前标签/合集上下文
- 文章详情页渲染 Markdown 内容（标题、段落、代码、表格、引用等）
- 文章详情页顶部阅读进度条 + 滚动触发 sticky 导航栏
- 阅读位置记忆，7天内再次打开弹窗提示继续阅读
- 文章底部上一篇/目录/下一篇导航（合集内隔离导航，单独文章排除合集）
- 合集详情页展示合集内所有文章卡片
- 页面切换 fade 过渡动画 + 合集页卡片展开动画
- 附件文件预览（PDF 使用 iframe 预览）

### 2.3 实时更新系统
- 开发模式下，后台修改文档时自动检测变化并推送到浏览器
- **正在查看的文章被修改时**，弹窗提醒"文档已更新"
- **阅读历史记录**：用 localStorage 记录每篇文章的阅读版本，再次打开已更新文章时弹窗提醒
- SPA 内导航到已更新文章时弹窗提醒，刷新页面时直接显示最新内容（不弹窗）
- 文章被删除时弹窗提醒并引导返回首页

### 2.4 内容保护（高强度）
- **CSS 层**：禁止文字选择、禁止拖拽、打印时隐藏内容
- **JS 层**：禁用右键菜单、拦截复制/查看源码/打印/DevTools 快捷键
- **水印层**：Canvas 动态水印覆盖全屏，显示时间戳 + 随机 ID
- **反调试层**：DevTools 尺寸检测 + debugger 时间差检测，检测到则模糊内容
- **内容加载保护**：内容 Base64 编码存储，JS 动态解码渲染
- **标签页检测**：切换标签页时内容模糊，防止后台截图
- **爬虫防护**：robots.txt 全站禁止、meta 标签禁止索引和缓存

### 2.5 主题系统
- 支持亮色/暗色主题切换
- 优先跟随系统偏好，支持手动切换
- localStorage 持久化用户选择

### 2.6 邀请码系统（访问控制）
- 支持**全站锁**：开启后所有页面需输入邀请码，全屏验证页面
- 支持**单篇锁**：文章旁放 `文件名.lock` 文件标记锁定，每篇可设独立邀请码
- 支持**合集锁**：合集文件夹内放 `.lock` 文件，整个合集共享一个邀请码；单篇 `.lock` 优先级高于合集 `.lock`
- 邀请码通过 SHA-256 哈希存储，配置文件和 `.lock` 文件中不含明文码
- `npm run gen-code` 随机生成邀请码 + 哈希，支持自定义码
- 验证通过后 localStorage 记住 7 天，到期需重新输入
- `src/config/invitation.js` 集中管理全站锁/单篇锁的 enabled 开关
- 全站锁和单篇锁可同时开启（双层保护）
- **锁定可视化**：首页卡片、合集卡片、文章导航、目录弹窗统一显示锁状态图标
  - 未解锁：amber 闭合锁图标
  - 已解锁：green 打开锁图标
- 导航跳转到锁定文章时弹出确认提醒
- 目录弹窗排序与首页全局排序同步
- 多标签文章的目录通过 sessionStorage 追踪标签上下文，确保 siblings 一致

### 2.7 移动端体验
- 移动端点击文章中心区域（中间 3/5）呼出浮层菜单
- 浮层顶部栏：返回箭头 + 字体大小调节（4档）
- 浮层底部栏：上一章 / 目录 / 下一章
- 标签筛选使用底部抽屉面板
- 文章锁住时导航仍可操作

### 2.8 访问模型
- 默认无邀请码时，网站完全公开，无需登录/密码
- 开启邀请码后，全站或特定文章需输入邀请码
- 所有访客只能查看，不能复制/修改/下载文字内容
- 站主通过本地文件管理 + 部署更新

## 3. 非功能需求

| 需求 | 说明 |
|------|------|
| 性能 | 首屏加载 < 2s，文章切换流畅 |
| 兼容性 | Chrome/Edge/Firefox/Safari 最新版 |
| 响应式 | 支持桌面和移动端 |
| SEO | 故意禁止 SEO（robots.txt + meta noindex），保护内容 |

## 4. 用户故事

### 站主
1. 我在 `content/` 下创建分类文件夹（如 `content/工作/`）
2. 我把 Word 文件拖进文件夹
3. 我运行 `npm run build` 构建静态站点
4. 我部署到 GitHub Pages / Vercel
5. 访客打开网站即可浏览我的文档

### 访客
1. 我打开网站，看到文章列表
2. 我点击左侧标签，按分类筛选文章
3. 我点击文章卡片，查看文章详情
4. 我尝试复制文字 → 无法选中
5. 我尝试右键 → 菜单被拦截
6. 我尝试 Ctrl+C → 快捷键被拦截
7. 我尝试打开 DevTools → 内容变模糊
8. 我切换标签页 → 内容变模糊

## 5. 版本规划

| 版本 | 内容 | 状态 |
|------|------|------|
| v0.1.0 | 基础框架 + 文章列表 + 标签筛选 + 文章详情 + 6 层内容保护 + 亮暗主题 | ✅ 已发布 |
| v0.5.0 | 文章底部上一篇/目录/下一篇导航 | ✅ 已发布 |
| v0.6.0 | 文章按标题排序 + 修复切换文章误弹更新弹窗 | ✅ 已发布 |
| v0.7.0 | txt 内容直接渲染 + 文档更新提醒重构（实时/历史） | ✅ 已发布 |
| v0.8.0 | docx 空行精确保留 + .doc 格式支持 + 空格换行保留 + 刷新不弹窗 | ✅ 已发布 |
| v0.9.0 | 目录弹窗改为底部滑出面板 | ✅ 已发布 |
| v0.10.0 | 标签筛选改为弹出面板 + 移动端底部抽屉 | ✅ 已发布 |
| v0.11.0 | 文章页阅读进度条 + 滚动触发 sticky 导航栏 | ✅ 已发布 |
| v0.12.0 | 阅读位置记忆 + 继续阅读提示 | ✅ 已发布 |
| v0.13.0 | 创建时间 + 更新时间双字段，按创建时间排序 | ✅ 已发布 |
| v0.14.0 | 全局标题搜索 | ✅ 已发布 |
| v0.15.0 | 合集功能（子文件夹自动识别） | ✅ 已发布 |
| v0.16.0 | 合集堆叠卡片混排展示 | ✅ 已发布 |
| v0.17.0 | 页面切换动画 + 合集页卡片展开效果 | ✅ 已发布 |
| v0.17.3 | 合集导航隔离 + 搜索上下文筛选 + 多标签（.tags 文件） | ✅ 已发布 |
| v0.18.0 ~ v0.19.0 | 左侧分类侧边栏 + 文章字数显示 | ✅ 已发布 |
| v0.20.0 | 移动端阅读浮层菜单 + 字体大小调节 | ✅ 已发布 |
| v0.21.0 | 邀请码系统（全站锁/单篇锁/合集锁）+ 锁状态可视化 + 目录排序同步 + 多标签导航修复 | 当前版本 |
